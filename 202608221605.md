# 2026-08-22 16:05

- 按最新 review 要求移除已废弃的 `lilac` 与 `memof` 依赖。
- 通过 `calcit config rm-module` 同步更新 canonical `calcit.cirru` 的 default entry 模块列表。
- 使用最新 Calcit 0.13.29 重新完成 caps、类型检查、JS codegen 和 CLI 编译验证。
