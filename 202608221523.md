# Calcit 0.13.29 migration follow-up

- Removed the legacy `compact.cirru` snapshot; `calcit.cirru` is the only project snapshot.
- Pinned direct Calcit module inputs to the verified upstream commits for reproducible resolution.
- Added canonical snapshot formatting/check-only validation to CI.
- Verified immutable Yarn install, JS generation, and the CLI build.
