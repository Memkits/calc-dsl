# 2026-08-22 18:18 — Release calc-dsl 0.0.4

- Published the merged Calcit 0.13.29 migration and review-comment fixes as 0.0.4.
- Replaced direct dependency hashes with formal Respo module versions and loaded the transitive `js-ffi/` module in the default entry so the current Snapshot check-only path is reproducible.
- Verified `caps tree`, canonical formatting, native check-only, JavaScript generation, immutable Yarn install, and `yarn compile-cli`.
